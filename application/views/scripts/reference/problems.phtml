<?php if (! $this->compact): ?>
<div class="controls">
    <?= $tabs ?>
</div>
<?php endif ?>
<div class="content">
<?php foreach ($services as $service): ?>
    <p><?= $this->escape(sprintf('%s: %s', $service->host_name, $service->service_description)) ?></p>
<?php endforeach ?>
</div>
